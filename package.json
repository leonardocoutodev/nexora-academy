{
  "name": "lc-learn-platform",
  "private": true,
  "version": "6.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "audit": "node scripts/audit.mjs",
    "test": "node --test",
    "check": "npm run audit && npm run quality:static && npm test && wrangler deploy --dry-run",
    "quality:static": "node scripts/quality-static.mjs",
    "postinstall": "node scripts/stamp-build.mjs",
    "predeploy": "node scripts/stamp-build.mjs"
  },
  "devDependencies": {
    "wrangler": "4.127.0"
  }
}
