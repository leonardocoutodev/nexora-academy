{
  "name": "lc-learn-platform",
  "private": true,
  "version": "6.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "audit": "node scripts/audit.mjs",
    "test": "node --test",
    "check": "npm run audit && npm test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.127.0"
  }
}
