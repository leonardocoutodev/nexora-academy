{
  "name": "nexora-academy-supabase",
  "private": true,
  "version": "5.1.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "audit": "node scripts/audit.mjs",
    "test": "node --test",
    "check": "npm run audit && npm test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.127.0"
  }
}
